<div class="min-h-screen bg-neutral-950 text-neutral-100">
  <div class="mx-auto max-w-6xl px-6 py-10">
    <div class="mb-8 flex items-center justify-between gap-6">
      <div>
        <div class="text-xs tracking-widest text-neutral-500">CELLGUARD CONTROL PLANE</div>
        <h1 class="mt-2 text-3xl font-semibold"><%= @title %></h1>
        <% if @subtitle.present? %>
          <div class="mt-2 text-sm text-neutral-400"><%= @subtitle %></div>
        <% end %>
      </div>

      <div class="flex items-center gap-3">
        <a href="/dashboard" class="rounded-xl border border-neutral-800 px-4 py-2 text-sm hover:bg-neutral-900">Dashboard</a>
        <a href="/incidents" class="rounded-xl border border-neutral-800 px-4 py-2 text-sm hover:bg-neutral-900">Incidents</a>
        <a href="/runbooks/gameday" class="rounded-xl border border-neutral-800 px-4 py-2 text-sm hover:bg-neutral-900">Runbooks</a>
      </div>
    </div>

    <%= content %>

    <div class="mt-10 border-t border-neutral-900 pt-6 text-xs text-neutral-600">
      Policy-as-code • Auditability • Closed-loop reliability
    </div>
  </div>
</div>
