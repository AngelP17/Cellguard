name: Deploy (Gated by Release Policy)

on:
  workflow_run:
    workflows: ["Gate Proof (Local Stack)"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy placeholder
        run: |
          echo "Gate Proof passed"
          echo "Deploy pipeline allowed by release policy proof."
